#import Perfil from 'perfil.graphql'

enum UsuarioStatus {
  ATIVO
  INATIVO
  BLOQUEADO
}

type Usuario {
  id: Int
  nome: String!
  email: String!
  idade: Int
  salario: Float
  vip: Boolean
  perfil: Perfil
  status: UsuarioStatus
}
